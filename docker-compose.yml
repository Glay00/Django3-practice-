version: '3.4'

services:
    firstApp:
      build:
          dockerfile: Dockerfile
      command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
      ports:
        - "8000:8000"
      depends_on:
        - db
    
    db:
      image: postgres
      my_container: postgres
      enviroment:
        POSTGRES_DB: LMCompany
        POSTGRES_USER: gleb
        POSTGRES_PASSWORD: 011020001
    
